ANTHROPIC_API_KEY=sk-...
PINECONE_API_KEY=...
PINECONE_INDEX=alien-db-uap
PINECONE_NAMESPACE=nuforc-full
# For full bulk seed, run with EMBEDDER_DEVICE=cpu (MPS first-encode can be slow/hang for e5-large on some setups).
# Use nuforc-v0.1-proto to target the original small verified sample instead.
# HF token only if private gated dataset (NUFORC public)
# HF_TOKEN=...
