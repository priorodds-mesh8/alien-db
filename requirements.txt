# v0.1 prototype (Gradio + NUFORC + xAI/Anthropic + Pinecone dense vectors + client embeddings)
datasets>=2.14
anthropic>=0.30
gradio>=4.0
pydantic>=2.0
python-dotenv
sentence-transformers>=3.0
tqdm>=4.0
# torch pulled as dep of sentence-transformers (CPU/MPS on mac, CUDA elsewhere)
# pinecone-client not required; we use direct REST for explicit vectors (matches our 1024-dim e5 index)
# Optional for better chunking / PDF later
# langchain-text-splitters
# pdfplumber
# pytesseract
# pillow
